{
  "name": "catholink",
  "version": "1.0.0",
  "description": "Catholics in Connection",
  "main": "index.js",
  "scripts": {
    "start": "node back/server.js",
    "start-db": "mongod --dbpath=./back/db-data --auth",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kevin Bruton",
  "license": "ISC",
  "devDependencies": {
    "husky": "^1.1.4"
  },
  "husky": {
    "hooks": {
      "pre-commit": "cd front && node scripts/test"
    }
  }
}
