{
  "name": "catholink",
  "version": "1.0.0",
  "description": "Catholics in Connection",
  "main": "index.js",
  "scripts": {
    "start": "node back/server.js",
    "start-db": "mongod --dbpath=./back/db-data --auth",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd front && node scripts/build.js",
    "build-start": "cd front && npm i && node scripts/build.js && cd ../back && npm i && node server.js",
    "postinstall": "cd front && npm install && cd ../back && npm install && cd ..",
    "heroku-start": "cd front && node scripts/build"
  },
  "author": "Kevin Bruton",
  "license": "ISC",
  "devDependencies": {
    "husky": "^1.1.4"
  },
  "husky": {
    "hooks": {
      "pre-commit": ""
    }
  }
}
